{
  "name": "headcount",
  "version": "1.0.0",
  "description": "Headcount Events Management Platform",
  "scripts": {
    "build-css": "npx @tailwindcss/cli -i ./public/css/tailwind-input.css -o ./public/css/tailwind-output.css --minify",
    "watch-css": "npx @tailwindcss/cli -i ./public/css/tailwind-input.css -o ./public/css/tailwind-output.css --watch",
    "copy-quill": "node scripts/copy-quill.js",
    "postinstall": "npm run copy-quill"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  },
  "dependencies": {
    "apexcharts": "^3.54.1",
    "quill": "^1.3.6"
  }
}
